rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /user/{uid} {
      allow read, write: if
          uid == request.auth.uid
    }
    match /Document {
      allow read, write: if
        true
    }
  }
}